/**
 * 더미 @matter/nodejs-ble — noble import 방지
 *
 * MatterController.js:23의 `import "@matter/nodejs-ble"`가
 * 이 파일을 로드. noble 없이 정상 시작.
 * 실제 BLE는 RemoteBle (install.js)이 담당.
 */
// 의도적으로 비어있음 — ServiceBundle 등록 안 함
export {};
